spring.application.name=idev-platform

server.port=8080

# H2 - Temporário (desabilitado)
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=samuel0909
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.h2.console.enabled=true

# SQL Server - Dados permanentes (ativo)
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=bd_IDev_Platform;encrypt=false;trustServerCertificate=true
spring.datasource.username=${DB_USERNAME:sa}
spring.datasource.password=${DB_PASSWORD:@ITB123456}
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
logging.level.org.hibernate.SQL=WARN

# CORS Configuration
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}

# File Upload Configuration
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
spring.servlet.multipart.enabled=true

# Security - Disable stack trace and error details in responses
server.error.include-stacktrace=never
server.error.include-message=never
server.error.include-binding-errors=never

# Security - Disable stack trace in error responses
server.error.include-stacktrace=never
server.error.include-message=always
server.error.include-binding-errors=never

# REATIVAR USUÁRIO: PATCH /api/v1/usuario/{id}/reativar
# Exemplo: curl -X PATCH http://localhost:8080/api/v1/usuario/1001/reativar

# integratedSecurity: (true) Indica que a conexo ser feita com as credenciais do Windows (false) Credencial do SQL Server
# encrypt: (true) Requer criptografia do Cliente (false) No requer criptografia do cliente
# trustServerCertificate: (true) instrui o aplicativo cliente a ignorar a validao do certitificado SSL/TSL do Servidor

#spring.jpa.hibernate.ddl-auto:
  # none: Banco de dados e tabelas dever ser criados com antecedncia
  # update: Banco de dados criado com antecedncia, tabelas podem ser criadas com antecedncia, as tabelas so atualizadas
  #         pelo cdigo (model) toda vez que vc altera as entidades
  # create-drop: Banco de dados deve ser criado com antecedncia, as tabelas so criadas automaticamente pelo cdigo (model ),
  #              entretanto, as tabelas so excludas toda vez que o servidor  "encerrado"
  # create: Banco de dados deve ser criado com antecedncia, toda vez que o servidor  "carregado" as tabelas so recriadas

  # spring.jpa.show-sql= (true) As interaes (DDL, DML) com o banco de dados so exibidas no console
  #                      (false) As interaes (DDL, DML) com o banco de dados no sero exibidas no console